# every-dollar-tools

## Quick Run

To run the python script in the devcontainer use the `quick-run.sh` script

## Run in the devcontainer

build devcontainer
run `python every-dollar-tools.py --help`